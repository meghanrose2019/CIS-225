
/**
 * A program for Rick Hammer to compute prices of desks.
 *
 * @author Meghan Reed
 * @version 2020.01.31
 */
public class Desks
{
    // The price of a desk.
    private int price;
    // The width of the desk
    private int width;
    // The length of the desk
    private int length;
    // The type of wood for desk
    

    /**
     * Constructor for objects of class Desks
     */
    public Desks()
    {
        // Desk Variables
        price = 200;
        return Total();
      
        
    }

    /**
     * Asks for length of desk inches
     */
    public int getLength()
    {
        return length;
    }
    
    /**
     * Asks for width of desk inches
     */
    public int getWidth()
    {
        return width;
    }
    
    /**
     * If statement for surface area
     */
    public int getArea()
    {
        if (width * length > 750)
            price = price + 50;
        else 
            return price;
    }
    
    /**
     * If statement for surface area
     */
    public int getWood()
    {
        if (wood = "p")
            return price;
        else if (wood = "o") 
            price = price + 125;
        else if (wood = "m")
            price = price + 150;
    }
}
